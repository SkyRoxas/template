mixin footerMenuItem(name,href)
  li.pet
    a(href=href)= name

footer#footer
  .container
    wrap.d-flex
      div.menu
        ul.d-flex
          +footerMenuItem('關於貝果bar','/')
          +footerMenuItem('使用條款','/')
          +footerMenuItem('隱私權政策','/')
          +footerMenuItem('聯絡我們','/')
