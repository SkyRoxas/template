mixin hosts
  li.col
    .field-image.d-flex.justify-content-center.mb-3
      div
        img(src="./images/hosts.png" width="150px")
    .field-title.mb-3
      h3.text-center
        span.underLine ARROW WU
    .field-user
      span id / 3849128732

.region-hosts.bg-second
  .container
    div.d-flex.justify-content-center.my-5
      h2.text-center.my-5
        span.underLine 熱門直播主
    ul.row.mb-5
      +hosts
      +hosts
      +hosts
      +hosts
    ul.row.mb-5
      +hosts
      +hosts
      +hosts
      +hosts
      +hosts
      +hosts
